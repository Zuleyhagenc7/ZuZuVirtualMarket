@{
    ViewData["Title"] = "Reset Password";
}
<div class="row">
    <div class="col-md-4 m-auto my-5">
        <h1>Reset Password</h1>
        <hr />
        <form asp-action="PasswordChange">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" name="user" value="@Context.Request.Query["user"]"/>
            <div class="form-group">
                <label for="Password" class="control-label"></label>
                <input for="Password" class="form-control" type="password" required />
            </div>

            <div class="form-group mt-2">
                <input type="submit" value="Change My Password" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>